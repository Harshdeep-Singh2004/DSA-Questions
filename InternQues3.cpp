// Q. Imagine a terrain having different height on different points. Some watch towers are located on the terrain which emit light towards their left and right. Your job is to compute which place is illuminated and which is not. 
//   You are given an array of N integers and another array of K integers from range [0 … N-1]. The first array describes the terrain - these are heights of the points. 
//   The second array contains positions of towers on the terrain. towers are sorted in ascending order by their positions. 
//   Compute a bit mask of length N (an array of integers or booleans) that will encode 1 (or true) on each position illuminated by the tower.
